# blocksense-contracts
This repository contains all cross-application models, enums, and utility types used to ensure strong typing and consistency between the BlockSense API and desktop client.  The BlockSense.Contracts project is distributed as a private NuGet package and provides a single source of truth for data contracts shared across the platform.
